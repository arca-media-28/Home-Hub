{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "dev:local": "concurrently -k -n api,web -c blue,green \"NODE_ENV=development PORT=${API_PORT:-5000} DATA_DIR=./data pnpm --filter @workspace/api-server run dev:watch\" \"PORT=${WEB_PORT:-3000} BASE_PATH=/ VITE_API_PROXY_TARGET=http://localhost:${API_PORT:-5000} pnpm --filter @workspace/homelab-dashboard run dev\""
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^10.0.3",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
